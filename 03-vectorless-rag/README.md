# 03 — Vectorless RAG

Retrieval without embeddings. Classic lexical methods (BM25, TF-IDF) and structured retrieval still beat dense vectors for many workloads — especially when queries are keyword-heavy or the corpus is small.

## Status

📋 Planned.
